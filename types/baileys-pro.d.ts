declare module 'baileys-pro' {
  // baileys-pro has the same API as @whiskeysockets/baileys
  // We'll use any types since the package doesn't have official TypeScript definitions
  const content: any;
  export = content;
  export default content;
}
